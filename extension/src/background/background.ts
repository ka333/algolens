// AlgoLens Background Service Worker
console.log('AlgoLens background worker initialized.');

// Placeholder listener for extension install/setup
chrome.runtime.onInstalled.addListener(() => {
  console.log('AlgoLens extension installed successfully.');
});
